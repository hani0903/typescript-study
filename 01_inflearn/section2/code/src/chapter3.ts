// object
let user: { id?: number; name: string } = {
    id: 1,
    name: '조하은',
};

// 옵셔널 프로퍼티
let dog: { name: string; color: string } = { name: '모리', color: 'white' };

// readonly 프로퍼티티
let config: { readonly apiKey: string } = {
    apiKey: 'MY API KEY',
};
